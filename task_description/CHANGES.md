# CHANGES.md — Журнал змін документації та проекту

---

## 2026-08-09 — Telegram-бот `@driver_car_bot` піднято в проді

### Що зроблено
- Створено реального бота через `@BotFather` — `@driver_car_bot`.
- На Pi в `~/vehicle_tracker_api/.env` додано `TELEGRAM_BOT_TOKEN` і
  `TELEGRAM_ADMIN_IDS`.
- Контейнери `api` і `bot` перестворено —
  `docker compose up -d --force-recreate bot api` (rebuild не
  знадобився: це backend-змінні через `env_file: .env`, а не
  `VITE_*`, які запікаються в образ на етапі збірки).
- Перевірено логи (`docker compose logs bot`) — `Start polling` без
  traceback, бот `@driver_car_bot id=8661686764` живий.
- Створено відсутній `task_description/STATE.md` (файл був згаданий у
  `README.md`/`AGENTS_GLOBAL.md`/кореневому `CLAUDE.md`, але ніколи не
  існував у репозиторії) з поточним станом проекту.

### Статус
Бот у проді, приймає `/start`. Наскрізний сценарій (реєстрація →
сповіщення адміну → підтвердження → прив'язка `Profile.driver`) ще не
перевірено вручну повністю. Frontend-частина (Mini App, кнопка на
сайті) — статус push-конфлікту з `origin/main` не перевірявся в цій
сесії, див. запис 2026-08-04 нижче.

---

## 2026-08-04 — Фаза 6-7 `cars` задеплоєна, Фаза 8 дописана в гайд, Telegram-бот водіїв

### Що зроблено
- Backend `cars` (Фаза 6-7) — знайдено й виправлено 6 багів від
  ручного набору коду з `DJANGO_CODING_GUIDE.md`, задеплоєно.
- `DJANGO_CODING_GUIDE.md` — дописана Фаза 8 (`products`/`customers`/
  `waybills` views+urls), перевірена наскрізь перед записом, код у
  репозиторії поки не набраний руками.
- Нова позапланова фіча — Telegram-реєстрація водіїв (бот, aiogram) і
  вхід через Telegram Mini App (`initData`, HMAC-перевірка),
  підтвердження заявки з вибором ролі прямо в Telegram. Детально —
  новий файл `TELEGRAM_BOT_SETUP.md` у корені `vehicle_tracker_api`.
- Прибрано дублікат `Driver.telegram_id` (`apps/cars`) — єдине джерело
  правди тепер `Profile.telegram_id` (`apps/accounts`, `unique=True`).

### Чому
Водії не мають корпоративної пошти, тому не можуть пройти звичайну
web-реєстрацію (username/email/пароль) — потрібен окремий шлях через
Telegram, де реєстрація зводиться до "поділитись контактом" однією
кнопкою.

### Статус
Backend задеплоєно на Pi. Frontend (Mini App сторінка + Telegram-кнопка
на сайті) закомічено локально, але push заблокований конфліктом з
`origin/main` — ще не розв'язано. Реальний бот через @BotFather ще не
створено — `TELEGRAM_BOT_TOKEN` порожній.

---

## 2026-07-09 — Міграція на v3 (React PWA)

### Причина
Перехід від Django-архітектури до сучасного React/TypeScript стеку для забезпечення мобільності водіїв (PWA) та кращої аналітики.

### Що змінено в документації

#### AGENTS_GLOBAL.md
- Замінено Django стек на React 18 + TS + Tailwind.
- Додано опис архітектурних принципів v3 (Ексклюзивність каналів, Режими водія, Палети).
- Оновлено структуру проєкту під React.

#### AI_AGENT_CONTEXT.md
- Описано основні TypeScript інтерфейси (`Car`, `WaybillRecord`, `RouteEvent`).
- Додано опис структури Mock-даних.
- Деталізовано бізнес-логіку розподілу витрат та ексклюзивності каналів.

#### CLAUDE.md
- Оновлено команди для npm/vite.
- Додано приклади коду для React FC, TanStack Query та Tailwind.

#### README.md
- Оновлено опис проєкту та структуру документації.
- Додано посилання на папку `transport/` як першоджерело специфікацій.

#### STATE.md (Новий файл)
- Створено файл для відстеження поточного прогресу та стану проєкту між сесіями.

---

## 2026-03-14 — Початкова переробка під проект Expenses
(Архівна версія Django-проекту)
